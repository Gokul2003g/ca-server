# Instructions for setting up Access Control List

Rename or create a new folder with name `acl/` in the root folder of project.

Create a acl.toml file inside like the sample file given with all users
